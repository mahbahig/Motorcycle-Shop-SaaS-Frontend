import { EnterNext } from './enter-next';

describe('EnterNext', () => {
  it('should create an instance', () => {
    const directive = new EnterNext();
    expect(directive).toBeTruthy();
  });
});
